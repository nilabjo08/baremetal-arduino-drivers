# baremetal-arduino-drivers
Bare-metal GPIO and UART drivers for Arduino UNO
The solution is uploaded to a public GitHub repository with two branches (GPIO_dev and UART_dev) containing bare-metal implementations of GPIO and UART drivers for Arduino UNO
